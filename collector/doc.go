// Package collector provides simple generic collections: a rolling collection
// that keeps the newest elements, a timed collection that drops old elements,
// a growable collection, a set, a stack and a data points collector with
// time window aggregations.
package collector
